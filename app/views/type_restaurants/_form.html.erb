<%= form_with(model: type_restaurant, local: true) do |form| %>
  <% if type_restaurant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type_restaurant.errors.count, "error") %> prohibited this type_restaurant from being saved:</h2>

      <ul>
        <% type_restaurant.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
